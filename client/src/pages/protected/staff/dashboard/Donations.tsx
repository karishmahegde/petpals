import DashboardComingSoon from "../../../../components/ui/dashboard/DashboardComingSoon";

// Donor role work is explicitly deferred to a future sprint — routing/nav
// scaffolded ahead of the feature per product direction.
const Donations = () => <DashboardComingSoon title="Donations" emoji="💝" />;

export default Donations;
