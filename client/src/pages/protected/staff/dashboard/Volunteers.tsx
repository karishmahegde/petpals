import DashboardComingSoon from "../../../../components/ui/dashboard/DashboardComingSoon";

// Volunteer role work is explicitly deferred to a future sprint — routing/
// nav scaffolded ahead of the feature per product direction.
const Volunteers = () => <DashboardComingSoon title="Volunteers" emoji="🤝" />;

export default Volunteers;
