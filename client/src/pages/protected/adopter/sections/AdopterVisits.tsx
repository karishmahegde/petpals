import DashboardHeading from "../../../../components/ui/DashboardHeading";

// "Visits" section of the adopter dashboard.
const AdopterVisits = () => (
  <div>
    <DashboardHeading
      title="Visits"
      emoji="🏠"
      message="Your scheduled shelter visits"
    />

    {/* Section content to follow */}
  </div>
);

export default AdopterVisits;
